% global nrF;
% function [struct4domain] = figLibSet(varargin)
% Returns struct to manage your plots (scale, figNumber, etc...) used at
% the end of main script

    struct4domain = [];
    struct4domain.log = 1;
    % varargin{1}
% end

% sth = 1